# Hunter-Agent 第一层“翻译与过滤层”智能体持续实施操作手册

> 日期：2026-08-28  
> 适用范围：Hunter-Agent 当前最新总体设计中的第 1 层“任务翻译与安全过滤层”  
> 目标：让工程智能体在现有 Hunter-Agent 仓库中持续完成第一层的设计落地、真实依赖接入、真实测试、缺陷修复和最终验收，并将结果接入统一任务合同与运行审计体系。

---

## 0. 你的身份与工作方式

你是 Hunter-Agent 项目的工程实施智能体。收到本操作手册后，请直接进入现有仓库检查、实现、测试、修复和复测流程，不要停留在方案讨论阶段。除遇到确实无法由仓库、系统环境、依赖文档或代码推断出的外部信息缺失外，不要因为普通工程选择反复询问用户。

你的工作原则如下：

1. 先检查现有仓库结构、当前分支、已有 `TaskSpec`、运行目录规范、日志结构、依赖管理方式和测试框架，再决定文件落点。
2. 优先复用现有代码和成熟开源组件，避免自行重写文件识别、归档解析、哈希算法等成熟能力。
3. 所有新增功能必须通过真实程序、真实文件和真实依赖执行验证，不能只用模拟对象或只检查函数是否能导入。
4. 每完成一个子模块立刻增加测试并运行；发现问题立即修复，然后重跑受影响测试。
5. 保持修改范围集中在第一层及其必要的合同、配置、测试和文档，不随意重构第二层、第三层或四个专业能力后端。
6. 第一层禁止执行用户上传的未知二进制，禁止自动运行脚本，禁止主动攻击网络目标，禁止在预处理阶段做复杂漏洞结论。
7. 第一层的核心任务是确定性预处理和任务合同生成。若输入需要深度逆向、漏洞挖掘、取证或渗透分析，只进行类型、元信息、目标和领域候选识别，随后交给后续层。
8. 如果某个可选依赖在当前环境无法安装，先完成可降级的主路径并记录原因；随后尝试容器、系统包、虚拟环境或兼容替代方式。不要因为一个可选组件失败就停止整个任务。
9. 不要自行提交、推送或改写 Git 历史，除非仓库已有明确自动提交约定。最终保留清晰的修改清单、测试结果和未解决项。
10. 只有在本手册“最终验收门槛”全部满足后，才可以报告第一层完成。

---

# 1. 第一层的准确职责

第一层名称：**任务翻译与安全过滤层**。

它负责把用户给出的自然语言、文件、压缩包、源码目录、日志、网络目标或其他证据材料，经过确定性、安全、可审计的预处理后转换成 Hunter-Agent 后续层能够统一消费的任务合同。

目标数据流固定为：

```text
用户输入
  ↓
输入规范化
  ↓
安全入口检查
  ↓
文件类型识别
  ↓
必要时安全解压 / 递归展开
  ↓
哈希计算
  ↓
轻量元信息提取
  ↓
目标与领域候选识别
  ↓
生成 TaskSpec
  ↓
交给 Hunter 后续监督器
```

第一层至少支持以下输入类型：

- 自然语言任务；
- 单个普通文件；
- 压缩包；
- 源码仓库或工程目录；
- 二进制程序；
- 固件或复杂容器文件；
- 日志文件；
- 网络抓包；
- 内存镜像或磁盘镜像；
- IP、域名、网址等网络目标；
- 其他证据材料。

第一层输出必须稳定、结构化、可复现，并保留必要的来源、哈希、类型、处理记录和安全约束信息。

---

# 2. 开工前必须完成的仓库审计

不要立刻创建大量新目录。先在当前仓库执行一次只读审计，并把结果记录到工作日志中。

至少检查：

```text
1. 当前仓库根目录和主要模块布局
2. 当前分支与工作区是否已有未提交修改
3. TaskSpec 当前定义位置与字段
4. AgentResult 当前定义位置与字段
5. runs/<task_id>/ 当前是否已经存在
6. events.jsonl / layer1_result.json / result.json / task.json 当前生成方式
7. 是否已有 hunter/safety、runtime、contracts 或类似模块
8. 当前 Python 版本、依赖管理方式、测试框架
9. 是否已有 file/libmagic、Magika、libarchive、unblob、YARA-X 等依赖
10. 是否已有输入解析、文件类型识别、哈希、解压或路径检查代码
```

输出一个简短审计记录，例如：

```text
runs/_implementation_notes/layer1_repo_audit.md
```

如果仓库已经有等价功能，优先扩展现有模块，避免重复实现。

---

# 3. 开源组件采用策略

第一层采用“轻量编排 + 成熟组件”的方式实现。完整的 Strelka 仅作为架构和扫描器组织方式参考，不作为 Hunter-Agent 第一层的强制运行依赖。

## 3.1 Strelka：架构参考

参考仓库：

```text
https://github.com/target/strelka
```

重点学习：

- 文件进入后的类型识别；
- 基于文件类型选择确定性处理器；
- 扫描器注册与路由；
- 哈希和元信息收集；
- 结构化结果输出。

请勿为了第一层完整部署 Strelka 的分布式服务、队列和缓存系统，除非现有 Hunter-Agent 已经采用该技术栈并能明显降低工作量。

## 3.2 文件类型主识别：libmagic / file

参考仓库：

```text
https://github.com/file/file
```

要求：

- 以真实文件内容为主要识别依据；
- 不依赖扩展名判断真实类型；
- 对扩展名伪装文件能够给出真实类型；
- 保存原始识别字符串、规范化类型和识别器来源。

如果 Python 侧调用库选择存在多种方案，应优先选当前平台稳定、可维护、依赖最少的方式。

## 3.3 辅助识别：Magika

参考仓库：

```text
https://github.com/google/magika
```

使用条件：

- 主识别器输出未知、普通数据、模糊文本或结果置信度不足；
- 需要进一步区分脚本、源码、文档、压缩格式等类型。

要求：

- Magika 只作为补充识别器；
- 记录其标签和分数；
- 主识别器和辅助识别器不一致时，两份结果都保留，并由规范化规则给出最终候选类型和冲突标记；
- 不允许静默覆盖冲突。

## 3.4 安全解压：libarchive / python-libarchive-c

参考仓库：

```text
https://github.com/libarchive/libarchive
https://github.com/Changaco/python-libarchive-c
```

必须开启或自行实现以下防护：

- 禁止 `../` 路径逃逸；
- 禁止绝对路径写出目标目录；
- 禁止符号链接或硬链接将内容写到沙箱之外；
- 最大解压文件数；
- 最大解压后总字节数；
- 最大单文件字节数；
- 最大递归深度；
- 最大处理时间；
- 同名覆盖策略明确；
- 所有输出必须被限制在当前任务专属工作目录内。

不得直接调用系统解压命令并无条件把未知压缩包展开到任意目录。

## 3.5 固件和复杂容器：unblob

参考仓库：

```text
https://github.com/onekey-sec/unblob
```

调用条件：

- 文件被识别为固件；
- 普通归档器无法解释；
- 发现嵌入式文件系统、固件镜像或复杂嵌套容器迹象。

要求：

- 作为按需能力调用；
- 仍然受工作目录、处理时间、深度和总大小约束；
- 保存 unblob 的结构化元信息；
- 不执行提取出的脚本和二进制。

## 3.6 哈希：Python 标准库

至少计算：

```text
SHA-256
```

可根据现有设计补充：

```text
MD5
SHA-1
```

其中 SHA-256 是证据稳定标识和主要审计标识。哈希必须针对原始输入；如果发生解压，也应对重要子文件计算 SHA-256 并建立父子关系。

## 3.7 规则化分类：YARA-X，可选增强

参考仓库：

```text
https://github.com/virustotal/yara-x
```

第一阶段不要求依赖 YARA-X 才能完成主流程。若当前环境和时间允许，可将其作为确定性标签增强能力，用于文件家族、可疑模式或证据分类。

要求：

- 规则命中属于“标签或候选线索”；
- 不直接把规则命中提升为漏洞结论、恶意归因或最终取证结论；
- 所有命中保留规则名、文件哈希和位置等可审计信息。

---

# 4. 推荐代码组织

先尊重现有仓库结构。如果没有等价模块，可采用以下最小组织方式：

```text
hunter/
├── intake/
│   ├── __init__.py
│   ├── models.py
│   ├── normalizer.py
│   ├── safety.py
│   ├── filetype.py
│   ├── archive.py
│   ├── metadata.py
│   ├── domain_rules.py
│   ├── taskspec_builder.py
│   └── pipeline.py
│
├── contracts.py                 # 若现有工程已经在此定义 TaskSpec，则复用
└── ...

tests/
└── intake/
    ├── fixtures/
    ├── test_normalizer.py
    ├── test_safety.py
    ├── test_filetype.py
    ├── test_archive.py
    ├── test_metadata.py
    ├── test_domain_rules.py
    ├── test_taskspec.py
    └── test_layer1_integration.py
```

如果当前仓库已有 `hunter/safety/`、`runtime/` 或其他相近模块，应把对应功能落入已有位置，不要为了匹配本手册机械重排工程目录。

---

# 5. 统一输入对象

建议内部先生成一个输入描述对象，至少包含：

```python
input_id
input_kind
original_value
resolved_path
source_name
size_bytes
created_at
workspace
```

`input_kind` 至少支持：

```text
natural_language
file
directory
archive
network_target
other
```

规范化阶段必须处理：

- 路径展开与规范化；
- 文件或目录是否存在；
- 是否位于允许读取范围；
- 网络目标字符串格式；
- 网址、域名、IPv4、IPv6 基本识别；
- 文件名和真实文件类型分离；
- 原始输入保留，禁止丢失用户任务文本。

---

# 6. 安全入口检查

这一模块必须在任何解压或复杂处理之前执行。

至少实现以下检查：

```text
允许读取路径范围
输入对象存在性
普通文件 / 目录 / 符号链接类型
原始文件大小
目录中文件数量上限
目录递归深度上限
工作目录隔离
处理超时
```

安全原则：

1. 对上传或外部证据只读处理；
2. 所有派生文件写入任务工作区；
3. 绝不覆盖用户原始证据；
4. 绝不自动执行未知文件；
5. 绝不自动加载未知动态库；
6. 绝不把解压内容写到工作区之外；
7. 失败时给出明确错误类型并留下审计记录。

建议统一错误类型，例如：

```text
INPUT_NOT_FOUND
SCOPE_VIOLATION
FILE_TOO_LARGE
TOO_MANY_FILES
ARCHIVE_PATH_TRAVERSAL
ARCHIVE_LINK_ESCAPE
ARCHIVE_TOO_LARGE
ARCHIVE_TOO_DEEP
UNSUPPORTED_FORMAT
PROCESS_TIMEOUT
DEPENDENCY_UNAVAILABLE
TYPE_CONFLICT
```

如果现有工程已经有错误枚举，扩展现有枚举即可。

---

# 7. 文件类型识别

实现一个统一文件识别结果对象，建议包含：

```python
path
sha256
extension
magic_raw
mime_type
normalized_type
magika_label
magika_score
conflict
metadata
```

`normalized_type` 至少需要覆盖：

```text
text
source_code
script
archive
elf
pe
mach_o
pcap
evtx
memory_image
disk_image
firmware
document
image
unknown
```

识别逻辑：

```text
文件存在
  ↓
读取有限头部 / 调用 libmagic
  ↓
标准化主类型
  ↓
主结果模糊时调用 Magika
  ↓
合并识别信息
  ↓
检测扩展名与真实类型冲突
  ↓
输出结构化 FileTypeResult
```

要求：

- 不因扩展名为 `.pdf` 就直接认定文档；
- 不因文件名无扩展名就判为未知；
- 文件识别失败不能导致整个任务崩溃；
- 原始识别结果必须保留，便于后续审计。

---

# 8. 安全解压与递归展开

归档处理必须放在独立任务工作区，例如：

```text
runs/<task_id>/artifacts/input/
runs/<task_id>/artifacts/extracted/
```

如果现有运行目录命名不同，遵循现有规范。

归档提取结果至少记录：

```python
archive_sha256
archive_type
extract_root
files_extracted
bytes_extracted
max_depth_reached
blocked_entries
warnings
children
```

每个子文件至少记录：

```text
相对路径
大小
SHA-256
识别类型
来源归档哈希
递归深度
```

必须在写文件前进行路径归一化和边界检查。对任何即将写出的目标路径，都应验证其解析后的绝对路径仍位于任务提取目录内部。

资源限制请放入集中配置，不要散落硬编码。可提供类似：

```yaml
layer1:
  max_input_bytes: 1073741824
  max_files: 10000
  max_extract_bytes: 2147483648
  max_single_file_bytes: 536870912
  max_archive_depth: 4
  max_processing_seconds: 120
```

具体默认值可根据当前机器资源调整，但测试必须能通过临时降低阈值安全触发各类限制。

---

# 9. 轻量元信息提取

这里只提取后续路由和审计需要的低成本事实，不做复杂安全分析。

建议至少提取：

## 普通文件

```text
文件名
相对路径
大小
SHA-256
主类型
MIME
修改时间（如果来源可靠）
扩展名与真实类型是否冲突
```

## 目录 / 源码工程

```text
文件总数
主要语言候选
顶层构建文件
包管理文件
是否存在 .git
是否存在 Dockerfile
是否存在常见工程清单文件
```

只做工程识别，不在这里运行构建、依赖安装或测试。

## 二进制

第一层只记录：

```text
ELF / PE / Mach-O 类型
架构候选
是否有符号信息（若可低成本读取）
基础文件头信息
```

复杂函数分析交给逆向能力层。

## 网络抓包 / 日志 /镜像

第一层只记录：

```text
识别类型
大小
哈希
基础格式信息
```

深度解析交给应急响应能力层。

---

# 10. 领域候选识别与评分

第一阶段采用确定性规则，禁止调用大模型进行第一层领域分类。

四个目标领域：

```text
pentest
vulnerability_research
dfir
reverse
```

评分可采用 0 到 1，允许多个领域同时得到分数，以支持混合任务。

建议基础规则：

## reverse

以下输入增加逆向分数：

- ELF；
- PE；
- Mach-O；
- 固件；
- 用户目标文字包含二进制分析、函数定位、反汇编、解包、程序行为分析等明确目标。

## dfir

以下输入增加应急响应 / 数字取证分数：

- pcap；
- EVTX；
- 内存镜像；
- 磁盘镜像；
- 系统日志集合；
- 用户目标涉及入侵调查、时间线、IOC、进程、持久化、事件响应等。

## vulnerability_research

以下输入增加漏洞挖掘分数：

- 源码仓库；
- 可构建的软件工程目录；
- 含编译配置、依赖清单或模糊测试配置的项目；
- 用户目标涉及漏洞发现、PoV、崩溃、模糊测试、代码审计等。

## pentest

以下输入增加渗透测试分数：

- IP；
- 域名；
- 网址；
- 用户目标涉及授权范围内的目标测试、服务识别、漏洞验证或指定评测目标。

评分要求：

1. 规则必须可解释；
2. 输出每个分数的理由；
3. 同时匹配多个领域时保留多个分数；
4. 不强行将模糊输入归入单一领域；
5. 低置信度时允许输出 `hybrid` 或 `unknown` 候选，具体是否作为正式 `domain` 值需遵循当前仓库合同。

如果当前 `TaskSpec` 已经具有 `domain_scores` 字段，直接填充；如果没有该字段，优先把评分放入 `metadata["domain_scores"]`，避免破坏现有接口。待核心合同统一时再集中迁移。

---

# 11. TaskSpec 生成

必须复用当前最新设计中的统一任务合同。基础字段至少包括：

```python
@dataclass
class TaskSpec:
    task_id: str
    domain: str
    target: str
    goal: str

    timeout: int = 1800
    budget: float | None = None
    workspace: str | None = None

    scope: dict = field(default_factory=dict)
    success_conditions: list = field(default_factory=list)
    metadata: dict = field(default_factory=dict)
```

如果仓库已有正式定义，以仓库当前定义为准，本手册不要求复制一份新类。

第一层至少要把以下信息写入任务合同或其 `metadata`：

```text
原始输入类型
输入对象路径或网络目标
输入 SHA-256
真实文件类型
MIME
提取出的子对象摘要
领域评分
领域评分理由
安全处理限制
处理警告
依赖降级信息
第一层处理耗时
```

`scope` 应至少能够表达：

```text
允许访问的目标
允许读取的文件路径
任务专属工作目录
时间预算
存储 / 文件数量 / 解压限制
禁止动作
```

第一层不要凭空发明复杂的 `success_conditions`。如果用户任务文字中有明确成功条件，则保留；如果没有，则填入最小可解释占位或留给后续监督器补全，具体遵循现有合同约定。

---

# 12. 运行日志与审计

第一层每个任务必须产生可审计事件。优先复用当前 Hunter-Agent 的：

```text
runs/<task_id>/task.json
runs/<task_id>/events.jsonl
runs/<task_id>/layer1_result.json
runs/<task_id>/artifacts/
runs/<task_id>/logs/
```

`layer1_result.json` 是第一层翻译与过滤收据；`result.json` 保留给后续正式专业 Agent 的
`AgentResult`，第一层不写入该文件。

建议事件类型至少包含：

```text
input_received
scope_checked
file_type_detected
type_conflict_detected
archive_extract_started
archive_entry_blocked
archive_extract_finished
hash_computed
metadata_extracted
domain_scored
taskspec_created
layer1_failed
layer1_completed
```

事件内容不得记录不必要的大块原始敏感文件内容。记录路径、哈希、大小、类型、规则命中和必要摘要即可。

---

# 13. 真实测试要求

这一部分是强制要求。测试完成标准不接受“函数能导入”“模拟对象返回正确”“写了若干单元测试”作为全部证据。

必须同时具备：

```text
单元测试
集成测试
真实依赖测试
真实文件测试
安全对抗测试
端到端 TaskSpec 测试
重复运行一致性测试
```

## 13.1 测试样本应由测试脚本安全生成或使用公开无害样本

至少准备以下真实样本：

### A. 普通文本

创建一个 UTF-8 文本文件，检查类型、大小、哈希和元信息。

### B. 源码工程

创建一个最小 Python 或 C 工程目录，包含源码和构建/项目文件，验证其能被识别为源码工程，并给 `vulnerability_research` 产生合理分数。

### C. 真实 ELF

在 Linux 环境编译一个最小 C 程序：

```c
#include <stdio.h>
int main(void) { puts("hunter-layer1-test"); return 0; }
```

测试要求：

- 识别为 ELF；
- 计算真实 SHA-256；
- 重命名为 `fake.pdf` 后仍识别为 ELF；
- 扩展名冲突被记录；
- `reverse` 分数明显高于普通文本。

禁止执行这个 ELF 作为第一层处理步骤；编译只发生在测试准备阶段。

### D. 普通 ZIP / TAR

真实创建带多个文件和子目录的归档，实际调用第一层解压流程，验证文件被写入任务工作区、哈希正确、父子关系正确。

### E. 路径穿越归档

安全生成包含类似：

```text
../../escape.txt
```

条目的测试归档。

要求：

- 提取流程拒绝危险条目；
- 工作区外不得生成 `escape.txt`；
- 事件日志中记录阻止原因；
- 任务流程不能因为单个危险条目导致不可控崩溃。

### F. 绝对路径归档

生成含绝对路径条目的测试归档，验证被拒绝。

### G. 符号链接逃逸归档

生成一个小型 TAR，其中包含指向工作区外部的符号链接，再包含试图通过该链接写出的成员。

要求：

- 工作区外没有任何写入；
- 危险链接或相关成员被拒绝；
- 日志能说明触发的安全规则。

### H. 文件数量限制

生成例如 20 个极小文件的归档，把测试配置的 `max_files` 临时设置为 10，验证第 11 个之前或达到阈值时安全停止。

### I. 解压总大小限制

生成若干小文件，总大小超过测试阈值即可，例如把测试阈值设置为 50 KiB，归档内容达到 100 KiB。

不要制造真正的大型压缩炸弹。测试目标是验证限制机制，使用低阈值即可。

### J. 递归嵌套限制

生成 3 到 5 层小型嵌套归档，把测试最大深度设为 2，验证超过深度后停止继续递归，同时保留已提取内容和警告。

### K. 网络目标

测试：

```text
127.0.0.1
localhost
example.com
https://example.com/
2001:db8::1
```

只做字符串规范化和目标分类，不主动访问这些地址。

### L. pcap 或其他 DFIR 类型样本

如果系统已有 `tcpdump`、Scapy 或公开无害 pcap fixture，可创建或使用一个极小 pcap，验证 `dfir` 路由得分。若当前环境无法生成，应保留测试接口并记录依赖条件，随后优先寻找本地可安全生成方式完成。

### M. 固件 / 复杂容器路径

如果 unblob 安装成功，至少使用一个安全、小型、可控的复杂容器或嵌套归档进行真实调用，确认结果被解析进结构化元信息。如果当前环境没有合适无害固件样本，可使用 unblob 官方或仓库自带的小型测试 fixture；不要随意下载来历不明的固件。

---

# 14. 端到端测试场景

至少完成以下端到端调用。具体命令入口应根据现有 Hunter-Agent 运行方式设计，建议最终提供类似：

```bash
python -m hunter.intake <input>
```

或：

```bash
python -m hunter.run --prepare-only <input>
```

不要为了符合示例强行新增命令，如果仓库已有统一运行入口，应接入已有入口。

## 场景 1：伪装二进制

输入：`fake.pdf`，真实内容为 ELF。

期望：

```text
识别 ELF
记录扩展名冲突
计算 SHA-256
reverse 得到高分
生成合法 TaskSpec
不执行二进制
```

## 场景 2：安全压缩包

输入：包含源码目录的 ZIP。

期望：

```text
安全解压
所有内容位于任务工作区
对子文件生成 SHA-256
识别源码工程
vulnerability_research 得到高分
生成合法 TaskSpec
```

## 场景 3：恶意路径压缩包

输入：包含 `../../escape.txt` 的归档。

期望：

```text
危险条目被阻止
工作区外没有写入
日志记录阻止事件
流程给出结构化失败或部分成功状态
```

## 场景 4：网络目标

输入：

```text
https://example.com/
```

期望：

```text
识别为网络目标
规范化 URL
pentest 得到高分
不主动发起网络扫描
生成合法 TaskSpec
```

## 场景 5：DFIR 文件

输入：小型 pcap 或其他已识别 DFIR 证据。

期望：

```text
识别正确类型
计算 SHA-256
dfir 得到高分
不做深层调查
生成合法 TaskSpec
```

---

# 15. 重复运行与可复现性

对以下至少三个输入连续运行 3 次：

```text
普通文本
伪装 ELF
普通压缩包
```

验证：

- SHA-256 完全一致；
- 文件类型结果一致；
- 领域评分一致；
- 任务合同除 `task_id`、时间戳、工作目录等天然变化字段外语义一致；
- 不出现随机调用大模型造成的领域判断漂移；
- 审计事件顺序稳定。

---

# 16. 安全测试后的文件系统检查

所有归档安全测试后必须额外执行文件系统检查，确认：

```text
任务工作目录以外没有测试归档写出的新文件
原始输入文件未被修改
没有执行未知二进制
没有产生越权符号链接
没有遗留失控后台进程
```

如果能够使用临时目录运行测试，优先使用临时目录，并在测试结束后清理。

---

# 17. 依赖降级策略

主流程需要保证：

```text
libmagic 可用 → 正常识别
Magika 不可用 → 主识别仍可工作，并记录辅助识别器缺失
unblob 不可用 → 普通归档处理继续工作，并把复杂固件处理标记为暂不可用
YARA-X 不可用 → 不影响第一层核心验收
```

任何依赖缺失必须产生可审计状态，禁止静默跳过。

第一层核心完成不得依赖 Strelka 全套服务。

---

# 18. 性能与资源观测

第一层以低成本确定性处理为目标。真实测试时记录：

```text
单文件识别耗时
哈希耗时
解压耗时
任务总处理耗时
提取文件数量
提取总字节数
峰值内存（方便获取时）
是否调用辅助识别器
是否调用复杂容器处理器
```

先记录基线数据，不为追求漂亮数字提前做复杂优化。若发现明显异常，例如小文件处理需要数十秒、模型被重复加载、同一个文件被多次完整哈希，应修复明显低效路径。

---

# 19. 禁止行为

实施过程中禁止：

1. 直接运行未知上传二进制；
2. 自动执行解压得到的脚本；
3. 自动安装解压目录中的依赖；
4. 根据压缩包内部 README 自动执行命令；
5. 第一层自动启动 nmap、sqlmap、Metasploit 等攻击工具；
6. 对网络目标主动探测；
7. 把大模型引入文件类型识别或安全路径判断；
8. 只根据扩展名判断文件类型；
9. 将规则命中直接当作最终安全结论；
10. 为了追求测试通过而关闭路径、大小、深度等安全限制；
11. 用大量 mock 代替真实依赖测试；
12. 修改四个专业能力后端的核心逻辑来迁就第一层。

---

# 20. 实施顺序

严格按下面顺序持续推进，每一步完成后立刻测试。

## 阶段 1：现状审计与接口冻结

完成：

```text
仓库审计
找到正式 TaskSpec
找到 runs 规范
确认测试框架
确认当前依赖
```

输出：

```text
layer1_repo_audit.md
```

## 阶段 2：输入规范化与安全边界

完成：

```text
路径规范化
scope 检查
工作目录隔离
资源限制模型
统一错误类型
```

立即运行路径和范围测试。

## 阶段 3：文件类型识别与哈希

完成：

```text
libmagic
Magika 降级/补充
SHA-256
类型冲突记录
```

立即运行文本、源码、ELF、伪装 ELF 测试。

## 阶段 4：安全解压

完成：

```text
普通归档提取
路径穿越防护
绝对路径防护
链接逃逸防护
文件数限制
总大小限制
递归深度限制
超时
```

立即运行全部归档安全测试。

## 阶段 5：复杂容器能力

完成：

```text
unblob 按需调用
结果解析
资源限制继承
依赖降级
```

真实调用至少一次。

## 阶段 6：轻量元信息与领域评分

完成：

```text
文件元信息
源码工程识别
二进制基础识别
DFIR 类型识别
网络目标识别
四领域规则评分
评分理由
```

立即运行四领域路由测试。

## 阶段 7：TaskSpec 生成与运行审计

完成：

```text
TaskSpec Builder
metadata
scope
事件日志
task.json
```

立即进行端到端测试。

## 阶段 8：全量回归与缺陷修复

运行所有：

```text
单元测试
集成测试
真实依赖测试
安全测试
端到端测试
三次重复测试
```

任何失败都要定位、修复并重新执行相关测试和全量测试。

## 阶段 9：文档与验收报告

输出：

```text
docs/LAYER1_TRANSLATION_FILTERING_IMPLEMENTATION.md
docs/LAYER1_TRANSLATION_FILTERING_TEST_REPORT.md
```

如果现有 docs 命名规范不同，遵循现有规范。

---

# 21. 最终验收门槛

只有以下项目全部满足，才允许报告第一层完成：

- [ ] 自然语言、文件、目录、压缩包和网络目标均能进入统一入口；
- [ ] 文件类型识别基于真实内容；
- [ ] ELF 改名为 `.pdf` 后仍能识别真实类型；
- [ ] SHA-256 真实计算且重复运行一致；
- [ ] 普通压缩包能够真实、安全提取；
- [ ] `../` 路径穿越测试被真实阻止；
- [ ] 绝对路径逃逸被真实阻止；
- [ ] 符号链接逃逸被真实阻止；
- [ ] 最大文件数量限制真实生效；
- [ ] 最大解压总大小限制真实生效；
- [ ] 最大递归深度限制真实生效；
- [ ] 工作目录外没有安全测试产生的越界文件；
- [ ] Magika 可用时完成真实辅助识别；
- [ ] Magika 不可用时主流程可降级运行；
- [ ] unblob 可用时完成至少一次真实调用；
- [ ] unblob 不可用时主流程可降级运行并留痕；
- [ ] 四领域评分由确定性规则产生并带理由；
- [ ] 网络目标分类不产生主动扫描；
- [ ] 第一层没有调用大模型进行文件识别或安全判断；
- [ ] 第一层没有执行任何未知上传二进制；
- [ ] 生成的 TaskSpec 能通过当前仓库合同校验；
- [ ] 运行日志可追踪输入、类型、哈希、解压、评分和 TaskSpec 生成过程；
- [ ] 端到端场景全部通过；
- [ ] 至少三个真实输入连续运行三次，结果语义稳定；
- [ ] 全量测试最终通过；
- [ ] 实现文档和真实测试报告已生成。

---

# 22. 最终测试报告必须包含的内容

`LAYER1_TRANSLATION_FILTERING_TEST_REPORT.md` 至少包含：

```text
1. 测试环境
2. Python / 系统版本
3. 实际安装并调用的开源组件及版本
4. 每类测试使用的真实文件说明
5. 各测试命令
6. 各测试结果
7. 路径穿越等安全测试的文件系统验证结果
8. 三次重复运行结果
9. 性能和资源基础数据
10. 依赖降级测试结果
11. 当前仍未解决的问题
12. 最终验收清单
```

禁止只写“全部通过”。应留下足够证据，使另一个开发者可以在干净环境中重复执行。

---

# 23. 最终实现文档必须说明的内容

`LAYER1_TRANSLATION_FILTERING_IMPLEMENTATION.md` 至少说明：

```text
输入入口
安全边界
文件类型识别顺序
辅助识别触发条件
安全解压机制
资源限制
复杂容器调用条件
元信息模型
领域评分规则
TaskSpec 映射关系
日志事件
依赖和降级策略
与后续 Hunter Supervisor 的接口
```

同时明确引用采用或参考的开源项目：

```text
Strelka
file / libmagic
Magika
libarchive / python-libarchive-c
unblob
YARA-X（如实际接入）
```

记录各项目实际使用方式，区分“参考架构”和“运行依赖”。

---

# 24. 给工程智能体的持续执行指令

从现在开始直接工作。先审计当前 Hunter-Agent 仓库，然后以最小侵入方式实现第一层。不要只给方案，不要在完成一两个函数后停止，也不要把“单元测试通过”当作项目完成。每完成一个模块就运行真实测试，遇到失败立即分析原因并修复；修复后重跑相关测试。依赖安装失败时尝试可行的系统包、虚拟环境或容器方案；可选依赖仍失败时实现明确降级并继续主流程。所有归档安全功能必须使用真实构造的安全测试样本验证，且测试结束后检查工作目录之外没有越界文件。所有文件类型识别必须至少包含真实 ELF 及“ELF 改名为 `.pdf`”的伪装测试。所有领域评分必须是确定性规则并给出理由。最终必须生成可校验的 `TaskSpec`、运行审计记录、实现说明和真实测试报告，并执行一次全量回归。只有第 21 节验收清单全部满足，才能向用户报告“第一层完成”。如果存在确实无法解决的外部阻塞，报告时必须给出已完成范围、失败命令、原始错误、已尝试方案、受影响验收项和继续处理所需的最小外部条件。

---

# 25. 本阶段最终交付物

完成后仓库中应能找到以下等价成果：

```text
第一层实际实现代码
第一层配置
第一层真实测试集
第一层端到端入口
TaskSpec 生成链路
运行事件日志
第一层实现文档
第一层真实测试报告
```

最终向用户汇报时，请按照以下顺序：

```text
完成了什么
实际接入了哪些开源组件
真实测试通过了什么
安全对抗测试通过了什么
TaskSpec 实际长什么样
有哪些依赖采取了降级
当前仍有什么问题
下一步如何接 Hunter Supervisor
```

不要只汇报代码行数、文件数量或“测试绿色”。重点给出真实功能、真实运行和真实安全边界的证据。
